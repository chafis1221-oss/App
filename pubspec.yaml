name: qris_monitor
description: Aplikasi monitoring notifikasi QRIS real-time
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  web_socket_channel: ^2.4.0
  audioplayers: ^5.2.1
  shared_preferences: ^2.2.2
  flutter_local_notifications: ^17.0.0
  flutter_foreground_task: 6.1.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
